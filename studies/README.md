# Studies

A study explains why experiments were run and what conclusions the evidence supports. Each study may contain:

- a hypothesis;
- pinned experiment specifications;
- analysis source;
- a report;
- an `evidence.lock.json` file referencing immutable external bundles.

Raw mutable run directories do not belong in Git.
