apiVersion: benchplane/v1alpha1
kind: Experiment
metadata: { name: wrong-model }
spec:
  provider: { kind: local }
  runtime: { kind: llamaCpp, model: user-model, outputTokens: 4 }
  workload: { profile: smollm2-chat-greedy-v1, requests: 1 }
  budget: { maximumCostUsd: 0 }
