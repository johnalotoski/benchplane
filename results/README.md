# Results

Only tiny, intentionally checked-in fixtures or report summaries belong here. Real evidence bundles should be stored immutably outside Git and referenced by digest from a study's `evidence.lock.json`.
