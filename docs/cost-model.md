# Cost model

Benchplane should report more than instance price. The eventual cost model must distinguish:

- estimated versus reconciled cost;
- successful versus failed acquisition attempts;
- interrupted partial attempts;
- valid versus invalid measurements;
- cost per requested run;
- cost per valid experiment;
- cost per SLO-compliant workload unit.

No cloud execution should begin without an explicit cost ceiling and hard lifetime.
